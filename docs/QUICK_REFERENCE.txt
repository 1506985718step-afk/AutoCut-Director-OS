╔══════════════════════════════════════════════════════════════════╗
║         AutoCut Director - 产品级设计快速参考                      ║
╚══════════════════════════════════════════════════════════════════╝

📚 核心文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. docs/UI_FLOW.md              - UI 流程设计（4 个页面）
2. docs/DSL_UI_MAPPING.md       - DSL ↔ UI 映射表
3. docs/UI_API_SEQUENCE.md      - UI → API 时序图
4. docs/PRODUCT_LEVEL_DESIGN.md - 产品级设计总结

🔧 核心文件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. config/ui_dsl_mapping.json   - 映射配置文件
2. app/core/ui_translator.py    - UI 翻译器实现

🎯 设计原则
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 用户是"导演"，系统是"剪辑助理"
✅ 用户永远不接触 job_id / endpoint / DSL
✅ 用户只表达意图，系统负责实现
✅ 配置驱动，易于维护和扩展

📱 UI 架构（4 个页面）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 新建剪辑（Create）
   - 上传视频
   - 选择平台（抖音/B站/YouTube/快手）
   - 选择风格（教学/情绪/爆款/Vlog）
   - 高级选项（节奏/字幕/音乐）

2. 剪辑进行中（Processing）
   - 时间线式进度
   - 预计剩余时间
   - 详细日志（折叠）

3. 预览与修改（Review）⭐ 最重要
   - 视频预览
   - 剪辑摘要（Hook/节奏/BGM/时长）
   - 意图式调整（更快/更狠/换音乐/更多字幕）

4. 导出与历史（Export & History）
   - 选择质量（1080p/4K）
   - 下载成片
   - 历史版本管理

🔄 UI → DSL 映射
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
UI 控件              DSL 字段                    LLM Prompt
────────────────────────────────────────────────────────────────
平台：抖音           meta.target_platform        "目标平台：douyin"
                    meta.aspect_ratio           "视频比例：9:16"

风格：爆款           -                          "抖音爆款风格：节奏快..."

节奏：快             editing_plan.pace          "激进剪辑，只保留核心..."
                    timeline[].trim_frames

字幕：标准           subtitles.style            "字幕密度：standard"
                    subtitles.highlight_keywords

音乐：情绪           music[].mood               "使用背景音乐，情绪：emotional"

调整：节奏更快       (重新生成 DSL)             "用户反馈：节奏太慢..."

🌐 API 端点（产品级）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
POST   /api/create-project              创建项目
GET    /api/projects/{id}/status        获取状态（轮询）
GET    /api/projects/{id}               获取详情
GET    /api/projects/{id}/preview       获取预览视频
POST   /api/projects/{id}/adjust        调整项目
POST   /api/projects/{id}/export        导出成片
GET    /api/projects/{id}/versions      获取版本列表
GET    /api/exports/{id}/download       下载成片

⚠️ 旧 API（开发者专用）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/api/ingest/*        素材预处理（用户不可见）
/api/analyze/*       场景分析（用户不可见）
/api/llm/*           LLM 生成（用户不可见）
/api/execute/*       Resolve 执行（用户不可见）
/api/jobs/*          Job 管理（用户不可见）

💡 使用示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Python 后端
from app.core.ui_translator import get_translator

translator = get_translator()

# 翻译平台
meta = translator.translate_platform("douyin")

# 构建 prompt
prompt = translator.build_initial_prompt(
    platform="douyin",
    style="viral",
    pace="fast",
    subtitle_density="standard",
    music_preference="emotional"
)

# 调整 prompt
adjustments = {"pace": "faster", "hook": "stronger"}
new_prompt = translator.build_adjustment_prompt(prompt, adjustments)

# JavaScript 前端
// 创建项目
const response = await fetch('/api/create-project', {
    method: 'POST',
    body: formData
});

// 轮询进度
const status = await fetch(`/api/projects/${projectId}/status`);

// 调整项目
await fetch(`/api/projects/${projectId}/adjust`, {
    method: 'POST',
    body: JSON.stringify({adjustments})
});

📋 下一步实现
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
高优先级：
□ 实现产品级 API（create/adjust/export）
□ 更新前端 UI（使用新 API）
□ 集成 UI 翻译器
□ 端到端测试

中优先级：
□ 版本管理系统
□ 优化用户体验（加载动画、实时进度）
□ 错误处理和提示

低优先级：
□ 批量处理
□ 模板管理
□ 用户账号系统

✅ 验收标准
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ 用户看不到任何技术术语
□ 用户只需表达意图
□ 用户可以轻松回溯版本
□ 所有映射关系在配置文件中
□ UI 和 DSL 完全解耦
□ 代码易于维护和扩展

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
版本: v1.5.0 | 日期: 2026-02-05 | 状态: 设计完成，待实现
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
